<%= form_for(game) do |f| %>
    <% if game.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

          <ul>
            <% game.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :date %>
      <%= f.text_field :date %>
    </div>

    <div class="field">
      <%= f.label :team1_id %>
      <%= f.collection_select :team1_id, Team.order(:name), :id, :name %>
    </div>

    <div class="field">
      <%= f.label :team2_id %>
      <%= f.collection_select :team2_id, Team.order(:name), :id, :name %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
